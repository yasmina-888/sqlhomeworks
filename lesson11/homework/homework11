Easy-Level Tasks

Show all orders placed after 2022 along with the names of the customers who placed them.

SELECT o.OrderID, c.FirstName || ' ' || c.LastName AS CustomerName, o.OrderDate
FROM Orders o
JOIN Customers c ON o.CustomerID = c.CustomerID
WHERE o.OrderDate > '2022-12-31';


Display the names of employees who work in either the Sales or Marketing department.

SELECT e.Name AS EmployeeName, d.DepartmentName
FROM Employees e
JOIN Departments d ON e.DepartmentID = d.DepartmentID
WHERE d.DepartmentName IN ('Sales', 'Marketing');


Show the highest salary for each department.

SELECT d.DepartmentName, MAX(e.Salary) AS MaxSalary
FROM Employees e
JOIN Departments d ON e.DepartmentID = d.DepartmentID
GROUP BY d.DepartmentName;


List all customers from the USA who placed orders in the year 2023.

SELECT c.FirstName || ' ' || c.LastName AS CustomerName, o.OrderID, o.OrderDate
FROM Orders o
JOIN Customers c ON o.CustomerID = c.CustomerID
WHERE c.Country = 'USA' AND YEAR(o.OrderDate) = 2023;


Show how many orders each customer has placed.

SELECT c.FirstName || ' ' || c.LastName AS CustomerName, COUNT(o.OrderID) AS TotalOrders
FROM Customers c
LEFT JOIN Orders o ON c.CustomerID = o.CustomerID
GROUP BY c.CustomerName;


Display the names of products that are supplied by either Gadget Supplies or Clothing Mart.

SELECT p.ProductName, s.SupplierName
FROM Products p
JOIN Suppliers s ON p.SupplierID = s.SupplierID
WHERE s.SupplierName IN ('Gadget Supplies', 'Clothing Mart');


For each customer, show their most recent order. Include customers who havent placed any orders.

SELECT c.FirstName || ' ' || c.LastName AS CustomerName, MAX(o.OrderDate) AS MostRecentOrderDate
FROM Customers c
LEFT JOIN Orders o ON c.CustomerID = o.CustomerID
GROUP BY c.CustomerID;

🟠 Medium-Level Tasks

Show the customers who have placed an order where the total amount is greater than 500.

SELECT c.FirstName || ' ' || c.LastName AS CustomerName, o.TotalAmount
FROM Orders o
JOIN Customers c ON o.CustomerID = c.CustomerID
WHERE o.TotalAmount > 500;


List product sales where the sale was made in 2022 or the sale amount exceeded 400.

SELECT p.ProductName, s.SaleDate, s.SaleAmount
FROM Sales s
JOIN Products p ON s.ProductID = p.ProductID
WHERE YEAR(s.SaleDate) = 2022 OR s.SaleAmount > 400;


Display each product along with the total amount it has been sold for.

SELECT p.ProductName, SUM(s.SaleAmount) AS TotalSalesAmount
FROM Sales s
JOIN Products p ON s.ProductID = p.ProductID
GROUP BY p.ProductName;


Show the employees who work in the HR department and earn a salary greater than 60000.

SELECT e.Name AS EmployeeName, d.DepartmentName, e.Salary
FROM Employees e
JOIN Departments d ON e.DepartmentID = d.DepartmentID
WHERE d.DepartmentName = 'Human Resources' AND e.Salary > 60000;


List the products that were sold in 2023 and had more than 100 units in stock at the time.

SELECT p.ProductName, s.SaleDate, p.StockQuantity
FROM Sales s
JOIN Products p ON s.ProductID = p.ProductID
WHERE YEAR(s.SaleDate) = 2023 AND p.StockQuantity > 100;


Show employees who either work in the Sales department or were hired after 2020.

SELECT e.Name AS EmployeeName, d.DepartmentName, e.HireDate
FROM Employees e
JOIN Departments d ON e.DepartmentID = d.DepartmentID
WHERE d.DepartmentName = 'Sales' OR e.HireDate > '2020-01-01';

🔴 Hard-Level Tasks

List all orders made by customers in the USA whose address starts with 4 digits.

SELECT c.FirstName || ' ' || c.LastName AS CustomerName, o.OrderID, c.Address, o.OrderDate
FROM Orders o
JOIN Customers c ON o.CustomerID = c.CustomerID
WHERE c.Country = 'USA' AND c.Address LIKE '4%';


Display product sales for items in the Electronics category or where the sale amount exceeded 350.

SELECT p.ProductName, p.Category, s.SaleAmount
FROM Sales s
JOIN Products p ON s.ProductID = p.ProductID
WHERE p.Category = 'Electronics' OR s.SaleAmount > 350;


Show the number of products available in each category.

SELECT p.Category, COUNT(p.ProductID) AS ProductCount
FROM Products p
GROUP BY p.Category;


List orders where the customer is from Los Angeles and the order amount is greater than 300.

SELECT c.FirstName || ' ' || c.LastName AS CustomerName, c.City, o.OrderID, o.TotalAmount
FROM Orders o
JOIN Customers c ON o.CustomerID = c.CustomerID
WHERE c.City = 'Los Angeles' AND o.TotalAmount > 300;


Display employees who are in the HR or Finance department, or whose name contains at least 4 vowels.

SELECT e.Name AS EmployeeName, d.DepartmentName
FROM Employees e
JOIN Departments d ON e.DepartmentID = d.DepartmentID
WHERE d.DepartmentName IN ('Human Resources', 'Finance') 
OR LENGTH(REPLACE(LOWER(e.Name), 'a', '')) + LENGTH(REPLACE(LOWER(e.Name), 'e', '')) + LENGTH(REPLACE(LOWER(e.Name), 'i', '')) + LENGTH(REPLACE(LOWER(e.Name), 'o', '')) + LENGTH(REPLACE(LOWER(e.Name), 'u', '')) <= LENGTH(e.Name) - 4;


Show employees who are in the Sales or Marketing department and have a salary above 60000.

SELECT e.Name AS EmployeeName, d.DepartmentName, e.Salary
FROM Employees e
JOIN Departments d ON e.DepartmentID = d.DepartmentID
WHERE d.DepartmentName IN ('Sales', 'Marketing') AND e.Salary > 60000
